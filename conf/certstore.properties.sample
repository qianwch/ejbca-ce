# ------------ Certificate store configuration ---------------------
# These configurations are used both for EJBCA and the Verification Authority (VA).

# Certificate store servlet enabled. If false there will be no servlet.
# Default is false.
#certstore.enabled=true
