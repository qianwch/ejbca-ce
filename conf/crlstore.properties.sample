# ------------ CRL store configuration ---------------------
# These configurations are used both for EJBCA and the Verification Authority (VA).

# CRL store servlet enabled. If false there will be no servlet.
# Default is false.
#crlstore.enabled=true
